# FTP Server and Client Configuration
# Environment variables for Cybersecurity Lab

# Server Connection Settings
FTP_HOST=127.0.0.1
FTP_PORT=2121

# Authentication
FTP_USER=labuser
FTP_PASSWORD=labpass123

# Server Directory Configuration
FTP_SERVER_ROOT=ftp_server_root

# User Permissions (pyftpdlib format)
# e = change directory (CWD, CDUP commands)
# l = list files (LIST, NLST, STAT, MLSD, MLST commands)
# r = retrieve file from the server (RETR command)
# a = append data to an existing file (APPE command)
# d = delete file (DELE command)
# f = rename file (RNFR, RNTO commands)
# m = create directory (MKD command)
# w = store file to the server (STOR, STOU commands)
# M = change mode/permissions (SITE CHMOD command)
# T = change modification time (SITE MTIME command)
FTP_PERMISSIONS=elradfmwMT

# Network Configuration for Multi-Machine Setup
# Change FTP_HOST to the server IP address when using separate machines
# Example: FTP_HOST=192.168.1.100

# Security Note:
# This configuration is for educational/lab purposes only
# Never use these credentials in production environments
# Always use encrypted connections (FTPS/SFTP) for production use
